# ========================================
# OCA Custom Modules Dependencies for Odoo 18.0
# ========================================
# These packages are installed at runtime in Docker containers
# See: infra/scripts/init-odoo-template.sh

# REST Framework (base_rest, fastapi)
fastapi>=0.110.0
python-multipart
ujson
a2wsgi>=1.10.6
parse-accept-language
cerberus
pyquerystring
apispec
pydantic>=2.0.0
typing-extensions

# Component Framework (component, component_event)
extendable>=0.0.4
contextvars

# Utilities
cachetools
packaging

# Auto Backup Module
pysftp
paramiko<4.0.0

# Report Modules (py3o)
py3o.formats
py3o.template

# Error Tracking (sentry module)
sentry-sdk>=2.0.0,<=2.22.0

# Filesystem Storage (fs_storage, fs_attachment_s3, storage_backend_s3)
# For Cloudflare R2 and S3-compatible storage
fsspec[s3]>=2024.5.0
s3fs
aiobotocore
boto3

# Note: Core Odoo dependencies are included in official odoo:18.0 image
# This file only contains additional packages needed for OCA modules
